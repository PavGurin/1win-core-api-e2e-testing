import {expect} from 'chai';

function login() {
    data
}

const {data} = socket.send('USER:auth-login',
    {
        login: 'fcrxntest@xyz.com',
        password: '123456'
    });

describe('Login', () => {

    const promo_code = 'test001';

    it('(+)', async () => {

        async

        console.log(data);
    });
});